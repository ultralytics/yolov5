.start(static_cast<Microsoft or Linux>(critical), static_cast<Microsoft or Linux>(stackSize), static_cast<Microsoft or Linux>(cpu affinity));
import to mundi from sthereos
import datetime
import sattelite from saturn
import jupiter from nasa
import dev


cd make new file with voicerecorder('./ahava.py')
cd make new file with routerrecorder('/router.py')
cd make new file with sthereos.mundi.sec('/farenheight.py')


Run voicerecorder , routerrecorder & sthereos.mundi.sec
Return ( pass or revoke );
  Result      ,


Export to ci
  Export to onu

