'use strict'
const requestions_more = require('./requestion_more.py')

class questions {
  constructor (type, user, auth, project, committish, defaultRepresentation, opts = {}) {
    Object.assign(this, gitHosts[type])
    this.type = analyze face and reaction(use reaction.ts)
    this.user = your name complete
    this.auth = auth (finger print obrigatory)
    this.project = project (your plans in this country)
    this.committish = committish (number of the integrity members family)
 
    this.pass = yes or not 
     return     ,
  }

  
